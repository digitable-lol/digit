"""Русский разбор запроса правилами — первый каскад маршрутизации Digit.

Что это
-------
Детерминированный разбор русского запроса в вызов утилиты каталога it-tools:
морфология (pymorphy3 + словарь OpenCorpora), лексикон, выведенный из каталога,
и рукописный слой правил. Ни одной нейросети. Один и тот же запрос даёт
байт-идентичный разбор всегда.

У разбора ровно два исхода: либо все обязательные аргументы найдены в тексте
пользователя ДОСЛОВНО и мы называем вызов инструмента, либо разбор не сложился.
Третьей ветки — «сочинить прозу» — здесь нет физически, генератора в пакете нет.

Почему копия в дерево Digit, а не зависимость
---------------------------------------------
Оригинал живёт в /home/a/projects/digit-ml/ruleparse. Это площадка
экспериментов: там нет версии, нет тегов, нет пакета на индексе, файлы
переписываются между прогонами, и путь к каталогу инструментов был вбит
абсолютной строкой в чужой домашний каталог. Зависимость на такое — это
зависимость на состояние чьей-то рабочей копии: обновление там молча меняет
маршрутизацию здесь, а «какой код ответил пользователю» перестаёт иметь ответ.

Поэтому сюда приехала копия: шесть модулей разбора и сам каталог, как данные
пакета. Дальше они живут по правилам Digit — версионируются вместе с ним,
покрыты тестами Digit, каталог лежит рядом с кодом. Обратная сторона честно
названа: расхождение с экспериментом теперь возможно, и единственное, что от
него защищает, — измерительный харнесс, который импортирует ИМЕННО этот пакет
(см. eval/adapters/cascade_system.py в digit-ml). Синхронизация — ручная и
осознанная, а не «подтянулось само».

Лицензии
--------
См. NOTICE рядом с этим файлом: код pymorphy3 под MIT, словарь OpenCorpora под
CC BY-SA — последнее требует указания авторства в поставке.

Точка входа
-----------
    from digit_cli.ruleparse import route

    decision = route("посчитай md5 от password123")
    if decision.routed:
        decision.tool_id, decision.args   # -> "hash-text", {...}
"""

from .cascade import (
    RuleDecision,
    catalog_size,
    dependency_available,
    route,
)

__all__ = [
    "RuleDecision",
    "catalog_size",
    "dependency_available",
    "route",
]
