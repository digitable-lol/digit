"""Генератор спецификаций FTS: обученная модель за настоящим компилятором.

Три части, и граница между ними — это граница ответственности:

* :mod:`~digit_cli.specgen.model` — обученная модель. Пишет текст, ни за что не
  отвечает;
* :mod:`~digit_cli.specgen.gate` — настоящий компилятор FTS. Отвечает за всё:
  compile → validate → testUtilities;
* :mod:`~digit_cli.specgen.pipeline` — путь от просьбы до ответа, включая
  повторы и отказ.

Кто пользуется: `digit spec` (человек в терминале) и инструмент `write_spec`
(агент внутри разговора). Оба ходят через ``pipeline.write_spec`` и печатают
``pipeline.render`` — другого входа нет намеренно, иначе одна из двух дверей
рано или поздно начнёт показывать непроверенный текст.
"""

from __future__ import annotations

__all__ = ["gate", "model", "pipeline"]
